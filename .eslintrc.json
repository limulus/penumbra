{
  "extends": ["@limulus"],
  "ignorePatterns": ["dist/**/*", "src/wasm", "wasm"],
  "overrides": [
    {
      "files": ["*.ts", "*.tsx", "*.d.ts"],
      "parserOptions": { "project": ["./tsconfig.json"] }
    }
  ],
  "rules": {
    "no-unused-expressions": "off"
  }
}
